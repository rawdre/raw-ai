// Visible site version — the rollback anchor (shown in the footer, git-tagged per release).
// SYSTEM RULE: every website carries a visible version so any release can be rolled back.
// Bump on each meaningful change and tag the repo `vX.Y.Z`.
export const SITE_VERSION = "1.1.0";
